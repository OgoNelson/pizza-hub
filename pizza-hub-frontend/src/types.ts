export interface OrderData {
  fullName: string;
  phone: string;
  email: string;
  deliveryAddress: string;
  pizzaType: string;
  pizzaSize: string;
  quantity: number;
  unitPrice: number;
  totalPrice: number;
}
